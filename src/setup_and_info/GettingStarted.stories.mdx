import { Meta, Description, DocsContainer } from '@storybook/addon-docs';

import text from './GettingStarted.md';

<Meta
  title="Setup & Info/GettingStarted"
  parameters={{
    docs: {
      container: ({ context }) => {
         return (
          <DocsContainer context={context}>
            {(
              <Description markdown={text} />
            )}
          </DocsContainer>
        );
      },
    },
  }}
/>
