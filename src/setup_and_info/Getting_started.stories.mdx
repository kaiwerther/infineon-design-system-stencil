import { Meta, Description, DocsContainer } from '@storybook/addon-docs';

import text from './Getting_started.md';

<Meta
  title="Setup & Info/Getting started"
  parameters={{
    docs: {
      container: ({ context }) => {
         return (
          <DocsContainer context={context}>
            {(
              <Description markdown={text} />
            )}
          </DocsContainer>
        );
      },
    },
  }}
/>
