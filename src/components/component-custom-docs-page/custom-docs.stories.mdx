import { Meta, Story, Description,
 } from '@storybook/addon-docs';
import primaryConfig from './primaryConfig.json';
import  Button  from './custom-docs.tsx';

<Meta title="Components/ButtonMdxStory" 
component={Button} args={{variant: "solid", color: "primary", size: "m", href: ""}}/>

export const DefaultTemplate = (args) => ({ template: `<custom-docs :variant="variant" :color="color" :size="size" :href="href"></custom-docs>` });

  <Story
    name="Default"
    args={{
      label: 'Label',
    }}
  >
    {DefaultTemplate.bind({})}
    test
  </Story>

