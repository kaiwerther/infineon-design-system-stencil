import { Meta, Story, Description,
 } from '@storybook/addon-docs';
import primaryConfig from './primaryConfig.json';
import  Button  from './custom-docs.tsx';

<Meta title="Components/CustomDocs2" 
component={Button} args={{variant: "solid", color: "primary", size: "m", href: ""}}/>

export const PrimaryTemplate = (args) => ({ template: `<custom-docs :variant="variant" :color="color" :size="size" :href="href"></custom-docs>` });

  <Story
    name="Primary"
    args={{
      label: 'Label',
    }}
  >
    {PrimaryTemplate.bind({})}
    test
  </Story>

